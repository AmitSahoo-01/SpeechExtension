This is an chrome extension that will generate subtitles for a video and export it as a video file with the subtitles embedded in it. It will also export the subtitles as asrt file.


## Features
- Generate subtitles for a video and export it as a video file with the subtitles embedded in it. It will also export the subtitles as asrt file.
- Generate subtitles for a video and export it as a video file with the subtitles embedded in it. It will also export the subtitles as asrt file.

## Features
- Generate subtitles for a video and export it as a video file with the subtitles embedded in it. It will also export the subtitles as asrt file.
- Generate subtitles for a video and export it as a video file with the subtitles embedded in it. It will also export the subtitles as asrt file.
- Generate subtitles for a video and export it as a video file with the subtitles embedded in it. It will also export the subtitles as asrt file.
- Generate subtitles for a video and export it as a video file with the subtitles embedded in it. It will also export the subtitles as asrt file.
- Generate subtitles for a video and export it as a video file with the subtitles embedded in it. It will also export the subtitles as asrt file.


### How to Install

1. Download or clone this repository.
2. Open Google Chrome and navigate to `chrome://extensions`.
3. Enable "Developer mode" (usually a toggle in the top-right corner).
4. Click the "Load unpacked" button.
5. Select the folder where you saved this extension.

### How to Use

1. Click the CaptionFlow icon in your Chrome toolbar.
2. Drag and drop a video file or click "Browse Files" to select one.
3. Wait for the "Whisper" model to load (first time only, ~300MB).
4. The extension will automatically process the video and generate subtitles.
5. Use the controls to customize the style (color, size, position).
6. Click "Export Video" to save the video with embedded subtitles, or "Export .SRT" to save the subtitle file only.

Created By Amit.